using UnityEngine;
using Sticmac.ObservableModels;

public class ModelListener : MonoBehaviour
{
    [SerializeField] IReadableModel<int> _model;
    // Start is called before the first frame update
    void Start()
    {
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
